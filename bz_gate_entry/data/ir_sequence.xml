<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- Sequences for gate entry -->
        <record id="seq_gate_entry" model="ir.sequence">
            <field name="name">Gate Entry</field>
            <field name="code">gate.entry</field>
            <field name="prefix">GT/</field>
            <field name="padding">5</field>
            <field name="company_id" eval="False"/>
        </record>

        <!-- Sequences for gate entry -->
        <record id="seq_gate_entry_with_date" model="ir.sequence">
            <field name="name">Gate Entry with Date</field>
            <field name="code">gate.entry.date</field>
            <field name="padding">3</field>
            <field name="company_id" eval="False"/>
        </record>

    </data>
</odoo>
